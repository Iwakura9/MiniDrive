{
  "name": "minidrive",
  "private": true,
  "scripts": {
    "setup": "npm ci --prefix mini-drive && npm ci --prefix mini-drive-web",
    "lint": "npm run lint --prefix mini-drive",
    "test": "npm test --prefix mini-drive -- --runInBand && npm test --prefix mini-drive-web -- --watch=false",
    "test:e2e": "npm run test:e2e --prefix mini-drive -- --runInBand",
    "build": "npm run build --prefix mini-drive && npm run build --prefix mini-drive-web"
  },
  "dependencies": {
    "@angular/cli": "^22.1.7",
    "@nestjs/cli": "^12.0.0",
    "jest": "^30.5.1",
    "jsonwebtoken": "^9.0.3"
  },
  "allowScripts": {
    "@parcel/watcher@2.6.0": true,
    "unrs-resolver@1.12.2": true
  }
}
